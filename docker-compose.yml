version: "3.9"

services:
  frontend:
    container_name: mi-app-reactdock
    build: .
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules